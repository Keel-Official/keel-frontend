# The dev server must run on port 5173: it is the only localhost origin in the
# Keel API's exact-match CORS allowlist. See README.

# Contract mock (Prism, static mode). Start it with:
#   npx @stoplight/prism-cli mock docs/api/keel-openapi.yaml -p 4010
NEXT_PUBLIC_KEEL_API_URL=http://localhost:4010

# Live API. Read-only, no key, no auth.
# NEXT_PUBLIC_KEEL_API_URL=https://api.keels.app/v1
